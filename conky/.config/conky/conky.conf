-- Conky, a system monitor https://github.com/brndnmtthws/conky
--
-- This configuration file is Lua code. You can write code in here, and it will
-- execute when Conky loads. You can use it to generate your own advanced
-- configurations.
--
-- Try this (remove the `--`):
--
--   print("Loading Conky config")
--
-- For more on Lua, see:
-- https://www.lua.org/pil/contents.html

-- conky.config = {
--     alignment = 'top_left',
--     background = false,
--     border_width = 1,
--     cpu_avg_samples = 2,
-- 	  default_color = 'white',
--     default_outline_color = 'white',
--     default_shade_color = 'white',
--     draw_borders = true,
--     draw_graph_borders = true,
--     draw_outline = false,
--     draw_shades = false,
--     use_xft = true,
--     font = 'DejaVu Sans Mono:size=12',
--     gap_x = 64,
--     gap_y = 71,
--     minimum_height = 5,
-- 	  minimum_width = 500,
--     net_avg_samples = 2,
--     no_buffers = true,
--     out_to_console = false,
--     out_to_stderr = false,
--     extra_newline = false,
--     own_window = true,
--     own_window_class = 'Conky',
--     own_window_transparent = true,	
--     own_window_type = 'override',
--     stippled_borders = 0,
--     update_interval = 1.0,
--     uppercase = false,
--     use_spacer = 'none',
--     show_graph_scale = false,
--     show_graph_range = false,
--     short_units = true,
--     double_buffer = true
-- }
-- 
-- conky.text = [[
-- ${color lightgrey}Date: $color${execi 30 TZ=Asia/Tokyo date +"%Y/%m/%d"}
-- ${color lightgrey}Tokyo: $color${execi 30 TZ=Asia/Tokyo date +"%H:%M"}
-- ${color lightgrey}Weather: $color${texeci 3600 curl "wttr.in/?format=%l:+%C+%t+%w"}
-- 
-- ${color lightgrey}BTC: $color${execpi 60 /home/syamaguc/bin/bitcoin.sh}
-- ${color lightgrey}USD/JPY: $color${execpi 60 /home/syamaguc/bin/usdjpy.sh}
-- 
-- $hr
--  ${color grey}Info: $color${scroll 32 $sysname $nodename $kernel $machine}
-- $hr
-- ${color grey}Uptime:$color $uptime
-- ${color grey}Frequency (in MHz):$color $freq
-- ${color grey}Frequency (in GHz):$color $freq_g
-- ${color grey}CPU Usage:$color $cpu% ${cpubar cup0}
-- ${color grey}RAM Usage:$color $mem/$memmax - $memperc% ${membar 4}
-- ${color grey}Swap Usage:$color $swap/$swapmax - $swapperc% ${swapbar 4}
-- $hr
-- ${color grey}File systems: $color${fs_used /}/${fs_size /} ${fs_bar 6 /}
-- ${color grey}Network:
-- ${color grey}IPv4: $color${texeci 3600 curl inet-ip.info}
-- ${color grey}IPv6: $color${texeci 3600 curl ifconfig.me}
-- ${color grey}RX: $color${execi 10 vnstat -i wlp6s0 --oneline | cut -d ";" -f5} ${color grey}TX: $color${execi 10 vnstat -i wlp6s0 --oneline | cut -d ";" -f6}
-- $hr
-- ${color grey}Process:$color $processes  ${color grey}Running:$color $running_processes
-- ${color grey}Name                 PID   CPU%   MEM%
-- ${color lightgrey} ${top name 1} ${top pid 1} ${top cpu 1} ${top mem 1}
-- ${color lightgrey} ${top name 2} ${top pid 2} ${top cpu 2} ${top mem 2}
-- ${color lightgrey} ${top name 3} ${top pid 3} ${top cpu 3} ${top mem 3}
-- ${color lightgrey} ${top name 4} ${top pid 4} ${top cpu 4} ${top mem 4}
-- $hr
-- ${if_existing /sys/class/power_supply/BAT0}${color grey}Battery: ${battery_percent BAT0} ${battery_bar BAT0}${endif}
-- ${if_existing /sys/class/power_supply/BAT0}${color grey}Status: $color${execi 60 cat /sys/class/power_supply/BAT0/status} ${endif}
-- $hr
-- ]]


---
conky.config = {

	--Various settings
	background = false, 							-- forked to background
	cpu_avg_samples = 2,						-- The number of samples to average for CPU monitoring.
	diskio_avg_samples = 10,					-- The number of samples to average for disk I/O monitoring.
	double_buffer = true,						-- Use the Xdbe extension? (eliminates flicker)
	if_up_strictness = 'address',				-- how strict if testing interface is up - up, link or address
	net_avg_samples = 2,						-- The number of samples to average for net data
	no_buffers = true,							-- Subtract (file system) buffers from used memory?
	temperature_unit = 'fahrenheit',			-- fahrenheit or celsius
	text_buffer_size = 2048,					-- size of buffer for display of content of large variables - default 256
	update_interval = 1,						-- update interval
	imlib_cache_size = 0,                       -- disable image cache to get a new spotify cover per song

	-- Placement (Conky on MIDDLE of THREE monitors at 1920x1080)
	--alignment = 'top_left',		                -- top_left,top_middle,top_right,bottom_left,bottom_middle,bottom_right,
	--gap_x = 3540,                               -- pixels between right or left border
	--gap_y = 70,									-- pixels between bottom or left border

    -- Placement (For SINGLE monitor users!)
	alignment = 'top_right',		            -- top_left,top_middle,top_right,bottom_left,bottom_middle,bottom_right,
	gap_x = 50,								-- pixels between right or left border
	gap_y = 70,								-- pixels between bottom or left border

    -- Size
    minimum_height = 200,						-- minimum height of window
	minimum_width = 260,						-- minimum width of window
	maximum_width = 260,						-- maximum width of window

	--Graphical
	border_inner_margin = 5, 					-- margin between border and text
	border_outer_margin = 5, 					-- margin between border and edge of window
	border_width = 0, 							-- border width in pixels
	default_bar_width = 260,					-- default is 0 - full width
	default_bar_height = 10,					-- default is 6
	default_gauge_height = 25,					-- default is 25
	default_gauge_width =40,					-- default is 40
	default_graph_height = 40,					-- default is 25
	default_graph_width = 153,					-- default is 0 - full width
	default_shade_color = '#000000',			-- default shading colour
	default_outline_color = '#000000',			-- default outline colour
	draw_borders = false,						-- draw borders around text
	draw_graph_borders = true,					-- draw borders around graphs
	draw_shades = false,						-- draw shades
	draw_outline = false,						-- draw outline
	stippled_borders = 0,						-- dashing the border

	--Textual
	format_human_readable = true,				-- KiB, MiB rather then number of bytes
	font  = 'Source Code Pro:bold:size=10',  	-- the default font used
	font2 = 'Raleway:bold:size=30',         	-- font for the time
	font3 = 'Ubuntu:size=14',               	-- font for the date
	font4 = 'Raleway:bold:size=9',              -- font for the keybindings heading
	max_text_width = 0,							-- 0 will make sure line does not get broken if width too smal
	max_user_text = 16384,						-- max text in conky default 16384
	override_utf8_locale = true,				-- force UTF8 requires xft
	short_units = true,							-- shorten units from KiB to k
	top_name_width = 21,						-- width for $top name value default 15
	top_name_verbose = false,					-- If true, top name shows the full command line of  each  process - Default value is false.
	uppercase = false,							-- uppercase or not
	use_spacer = 'none',						-- adds spaces around certain objects to align - default none
	use_xft = true,								-- xft font - anti-aliased font
	xftalpha = 1,								-- alpha of the xft font - between 0-1

	--Windows
	own_window = true,							-- create your own window to draw
	own_window_argb_value = 255,			    -- real transparency - composite manager required 0-255
	own_window_argb_visual = true,				-- use ARGB - composite manager required
	own_window_class = 'Conky',					-- manually set the WM_CLASS name for use with xprop
	own_window_colour = '#282a36',				-- set colour if own_window_transparent no
	own_window_transparent = true,				-- if own_window_argb_visual is true sets background opacity 0%
	own_window_title = 'awesome_conky',			-- set the name manually
	own_window_type = 'override',				-- if own_window true options are: normal/override/dock/desktop/panel
	own_window_hints = 'undecorated,below,above,sticky,skip_taskbar,skip_pager',  -- if own_window true - just hints - own_window_type sets it

	--Colors (Dracula)
	color0 = '#bd93f9',
	color1 = '#f8f8f2',
	color2 = '#8be9fd',
	color3 = '#50fa7b',
  color4 = '#ffb86c',
  color5 = '#ff79c6',
};

conky.text = [[
${alignc}${color0}${font2}${time %H:%M}${font}
${voffset 6}${alignc}${color0}${font3}${time %b %d, %Y}${font}${color}

${color1}Uptime: ${color5}$uptime
${color1}${color0}${scroll 32 $sysname $nodename $kernel $machine}
${voffset 18}${goto 12}${color3}CPU${goto 50}$cpu%
${color2}${goto 12}${cpubar 8,254}
${voffset 5}${goto 12}$font${color1}${top name 1}$alignr$color${top cpu 1}%
${goto 12}${color1}${top name 2}$alignr$color${top cpu 2}%
${goto 12}${color1}${top name 3}$alignr$color${top cpu 3}%
${voffset 14}${goto 12}${color3}RAM${goto 50}$mem/$memmax
${color2}${goto 12}${membar 8,254}${color}
${goto 12}${voffset 5}${color1}${top_mem name 1}$alignr$color${top_mem mem_res 1}
${goto 12}${color1}${top_mem name 2}$alignr$color${top_mem mem_res 2}
${goto 12}${color1}${top_mem name 3}$alignr$color${top_mem mem_res 3}
${voffset 14}${goto 12}${color3}STORAGE${goto 75}$fs_used/$fs_size
${color2}${goto 12}${fs_bar 8,254}${color}
${color1}/var $alignr${color0}${execi 60  du -sh /var 2>/dev/null | awk '{print $1}'}
${color1}/tmp $alignr${color0}${execi 60  du -sh /tmp 2>/dev/null | awk '{print $1}'}
${color1}~/.cache $alignr${color0}${execi 60  du -sh ~/.cache 2>/dev/null | awk '{print $1}'}
${voffset 14}${goto 12}${color3}SWAP${goto 50}$swap/$swapmax
${color2}${goto 12}${swapbar 8,254}${color}

${color3}Network:
${color1}IPv4: ${color4}${texeci 3600 curl inet-ip.info}
${color1}IPv6: ${color4}${texeci 3600 curl ifconfig.me}
${color1}RX: ${color2}${execi 10 vnstat -i wlp6s0 --oneline | cut -d ";" -f5} ${color1}TX: ${color2}${execi 10 vnstat -i wlp6s0 --oneline | cut -d ";" -f6}

${color1}${hr}${color}

${color1}BTC: ${color4}${execpi 60 /home/syamaguc/bin/bitcoin.sh}
${color1}USD/JPY: ${color4}${execpi 60 /home/syamaguc/bin/usdjpy.sh}
${color1}pacman: ${color5}${execpi 3600 sudo pacman -Qu | wc -l}
${color1}yay: ${color5}${execpi 3600 yay -Qu | wc -l}
]];

